部署建议：
    1、为避免出现内存溢出，尽量细分路径，保持每个路径下的总文件个数在10000以内，或者更少
    2、系统部署完成后，应该先进行初始化传输，在开启定时任务进行增量传输
    3、考虑到并发问题，如果有多台客户端要传输文件，请先按顺序全量传输，然后增量，并且每个客户端的定时任务最好隔开，例如间隔1个小时
    4、考虑到系统cpu和内存使用问题，请设置好开启传输的线程数量
    5、未完待续。。。